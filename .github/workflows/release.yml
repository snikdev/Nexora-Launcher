name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "NexoraLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/NexoraLauncher-source NexoraLauncher-${{ env.VERSION }}
          mv NexoraLauncher-Linux-Qt6-Portable*/NexoraLauncher-portable.tar.gz NexoraLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv NexoraLauncher-Linux-aarch64-Qt6-Portable*/NexoraLauncher-portable.tar.gz NexoraLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv NexoraLauncher-*.AppImage/NexoraLauncher-*-x86_64.AppImage NexoraLauncher-Linux-x86_64.AppImage
          mv NexoraLauncher-*.AppImage.zsync/NexoraLauncher-*-x86_64.AppImage.zsync NexoraLauncher-Linux-x86_64.AppImage.zsync
          mv NexoraLauncher-*.AppImage/NexoraLauncher-*-aarch64.AppImage NexoraLauncher-Linux-aarch64.AppImage
          mv NexoraLauncher-*.AppImage.zsync/NexoraLauncher-*-aarch64.AppImage.zsync NexoraLauncher-Linux-aarch64.AppImage.zsync
          mv NexoraLauncher-macOS*/NexoraLauncher.zip NexoraLauncher-macOS-${{ env.VERSION }}.zip
          mv NexoraLauncher-macOS*/NexoraLauncher.dmg NexoraLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf NexoraLauncher-${{ env.VERSION }}.tar.gz NexoraLauncher-${{ env.VERSION }}

          for d in NexoraLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NexoraLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NexoraLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in NexoraLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NexoraLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NexoraLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in NexoraLauncher-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NexoraLauncher-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NexoraLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Nexora Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            NexoraLauncher-Linux-x86_64.AppImage
            NexoraLauncher-Linux-x86_64.AppImage.zsync
            NexoraLauncher-Linux-aarch64.AppImage
            NexoraLauncher-Linux-aarch64.AppImage.zsync
            NexoraLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            NexoraLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            NexoraLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            NexoraLauncher-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            NexoraLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            NexoraLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            NexoraLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            NexoraLauncher-macOS-${{ env.VERSION }}.zip
            NexoraLauncher-macOS-${{ env.VERSION }}.dmg
            NexoraLauncher-${{ env.VERSION }}.tar.gz
